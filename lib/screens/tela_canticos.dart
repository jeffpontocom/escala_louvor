import 'package:flutter/material.dart';

class TelaCanticos extends StatelessWidget {
  const TelaCanticos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('CANTICOS'),
    );
  }
}
